---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Amazon Account"
metadata:
  keywords:
    - "account"
    - "aws"
    - "amazon"
  categories:
    - "cloud"
  status: "stable"
  config:
  - "quarkus.account."
